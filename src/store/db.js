const USERS = require('./mocks/user');
const REVIEWS = require('./mocks/reviews');
const SERVICES = require('./mocks/services');
const AVAILABILITY = require('./mocks/availability');
const META = require('./mocks/meta');

module.exports = {
  USERS,
  REVIEWS,
  SERVICES,
  AVAILABILITY,
  META
};
